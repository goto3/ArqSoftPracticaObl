{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "compose-up-dev",
			"command": "docker-compose -f docker-compose.debug.yml up -d --build",
			"presentation": {
				"reveal": "always",
				"panel": "new"
			}
		}
	]
}
